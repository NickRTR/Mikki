{
	"name": "mikki",
	"version": "1.0.0",
	"author": "Glowman554 <glowman434@gmail.com>, NickRTR <contact@nickreutlinger.de>",
	"description": "Mikki ist ein Wiki, erstellt um die Assemblerprogrammierung im Schulunterricht zu dokumentieren.",
	"scripts": {
		"dev": "node write_github_commit.js && vite dev",
		"build": "node write_github_commit.js && vite build",
		"preview": "node write_github_commit.js && vite preview",
		"lint": "prettier --check .",
		"format": "prettier --write .",
		"tauri": "npm run build && tauri dev",
		"tauri:package": "npm run build && tauri build"
	},
	"devDependencies": {
		"@sveltejs/kit": "next",
		"prettier": "^2.6.2",
		"prettier-plugin-svelte": "^2.7.0",
		"svelte": "^3.54.0",
		"vite": "^4.0.0",
		"@sveltejs/adapter-static": "next",
		"@tauri-apps/cli": "^1.0.0-beta.10",
		"@zerodevx/svelte-toast": "^0.6.3",
		"concurrently": "^6.0.0",
		"svelte-preprocess": "^4.0.0"
	},
	"type": "module",
	"dependencies": {
		"svelte-markdown": "^0.2.2"
	}
}
